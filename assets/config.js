// Public configuration only. No integration secrets, GA4 tags or account keys.
window.WHITE_SWAN_CONFIG = Object.freeze({
  intakeOrigin: '', // Set only after the public intake host is deployed and verified.,
  analyticsEnabled: false
});
